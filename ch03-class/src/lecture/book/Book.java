package lecture.book;

public class Book {
        // author, title, pageCount, coverType
        //필드

        //생성자
        //기본생성자 ( 매개 변수 가 없는 생성자) -> 9클래스 이름이랑 같아야함!!!!+ public으로 해야함
        //생성자 메소드는 반드시 클래스의 이름과 동일해야한다.(대/소문자까지)
        //생성자 메소드는 반환형을 작성하지 않는다. (작성하게되면 일반 메소드로 인식한다)

        //art+insert ???

        //기본 생성자 (매개변수가 없는 생성자)
        //-> 매개변수가 있는 생성자가 하나라도 있으면
        // Compiler가 자동으로 기본생성자를 만들어주지 않는다.


        //메소드
        public String getTitle() {
            System.out.println("책의 작가는");
        }

}
